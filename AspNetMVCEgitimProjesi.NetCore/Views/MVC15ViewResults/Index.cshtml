@{
    ViewBag.Title = "Mvc View Result Çeşitleri";
}

<h2>Mvc View Result Çeşitleri</h2>

<h3>
    Redirect Result : Sayfayı bir url adresine yönlendirme için kullanılır
</h3>

@Html.ActionLink("Redirect Result", "Yonlendir")

<h3>
    RedirectToAction Result : Sayfayı başka bir actiona yönlendirme için kullanılır
</h3>

@Html.ActionLink("RedirectToAction Result", "Yonlendir")

<hr />

<h3>
    RedirectToRouteResult Result : Sayfayı başka bir route a yönlendirme için kullanılır
</h3>

@Html.ActionLink("RedirectToRouteResult Result", "RouteYonlendir")

<hr />

<h3>
    PartialViewResult : Actiona gelen istek sonucu geriye bir PartialView döndürmek için kullanılır
</h3>

@Html.Partial("_KategorilerPartial")

<h3>
    Kategorileri Action ile Getir : controller daki bir action u çalıştırmak için kullanılır
</h3>

@*@Html.Action("KategorileriGetirPartial")*@ NetCore da Html.Action metodu desteklenmiyor.

<a asp-action="KategorileriGetirPartial"></a>

<input type="submit" id="btnKategorileriGetirPartial" value="Kategorileri Getir" class="btn btn-warning" />
<br />
<div id="Kategoriler"></div>

@section scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            $("#btnKategorileriGetirPartial").click(function () {
                $.ajax({
                    url: '@Url.Action("KategorileriGetirPartial","MVC15ViewResults")',
                    type: 'GET',
                    dataType: 'html',
                    success: function (data) {
                        $("#Kategoriler").html(data);
                    },
                    error: function () {
                        alert("Hata alındı.");
                    }
                });
            });
        });
    </script>
}

<hr />

<h3>
    FileStreamResult ile Dosya İndirme : controller daki bir actiona gelen istek sonucu siteden dosya indirme işlemi yaptırmamızı sağlar.
</h3>

@Html.ActionLink("File Stream Result", "MetinDosyasiIndir")


<h3>
    JsonResult : controller daki bir actiona gelen istek sonucu geriye bir json türünde veri döndürebiliriz.
</h3>

<a asp-action="JsonDondur">JsonResult</a>

<h3>
    ContentResult : controller daki bir actiona gelen istek sonucu geriye bir xml türünde veri döndürebiliriz.
</h3>

@Html.ActionLink("Xml Content Result", "XmlContentResult")